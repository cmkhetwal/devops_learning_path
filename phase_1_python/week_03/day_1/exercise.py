"""
Week 3, Day 1: Exercise - Working with Lists

Practice creating lists, accessing elements by index, and using len().
All tasks are DevOps themed -- think servers, IPs, and infrastructure!

Run `python3 check.py` when you're done to check your answers.
"""

# --------------------------------------------------
# TASK 1: Create a list of server hostnames
# --------------------------------------------------
# Create a variable called `servers` that is a list containing
# exactly these 5 hostnames (in this order):
#   "web-01", "web-02", "db-01", "cache-01", "monitor-01"

# YOUR CODE HERE


# --------------------------------------------------
# TASK 2: Access specific servers by index
# --------------------------------------------------
# Using your `servers` list from Task 1:
#   - Create a variable `first_server` that holds the FIRST item
#   - Create a variable `last_server` that holds the LAST item (use negative indexing)

# YOUR CODE HERE


# --------------------------------------------------
# TASK 3: Find the fleet size
# --------------------------------------------------
# Create a variable called `fleet_size` that holds the number
# of servers in your `servers` list (use len()).

# YOUR CODE HERE


# --------------------------------------------------
# TASK 4: Create a list of open ports
# --------------------------------------------------
# Create a variable called `open_ports` that is a list containing
# these port numbers (in this order): 22, 80, 443, 8080, 3306

# Then create a variable called `ssh_port` that holds the FIRST
# port in the list (index 0).

# Then create a variable called `db_port` that holds the LAST
# port in the list (use negative indexing).

# YOUR CODE HERE


# --------------------------------------------------
# TASK 5: Update a server in the list
# --------------------------------------------------
# The server "web-02" needs to be replaced with "web-02-upgraded".
# Change the item at index 1 in your `servers` list to "web-02-upgraded".
# Then create a variable called `updated_server` that holds the
# value at index 1 of the `servers` list.

# YOUR CODE HERE
